{
  "name": "qiankun-link",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start-all": "npm-run-all --parallel start:*",
    "install-all": "npm-run-all --serial install:*",
    "install:main": "cd main && yarn",
    "install:subapp": "cd subapp && yarn",
    "start:main": "cd main && yarn run start",
    "start:subapp": "cd subapp && yarn run serve"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
